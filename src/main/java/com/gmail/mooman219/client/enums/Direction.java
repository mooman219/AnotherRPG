package com.gmail.mooman219.client.enums;

public enum Direction {
    UP(0),
    DOWN(1),
    LEFT(2),
    RIGHT(3);

    public final int index;

    Direction(int index){
        this.index = index;
    }
}
